# Visit https://goreleaser.com for documentation on how to customize this
# behavior.
version: 2

# Pin ProjectName explicitly so release artifacts are named
# `terraform-provider-burnham_v<version>` regardless of where the repo is
# cloned (local working dir vs. CI checkout dir).
project_name: terraform-provider-burnham

before:
  hooks:
    - go mod tidy
    # Generate only the embedded assets the binary needs: the WASM engines (resvg, fancy-regex), the bundled fonts, and the packed Pi table, all of which live under internal/provider.
    # We deliberately do NOT run the root `go generate ./...` here: that also fires the tfplugindocs directive in main.go, which `go build`s the provider and would fail on the not-yet-generated `go:embed` targets (a chicken-and-egg), and the generated docs are committed to the tree already so a release does not need to regenerate them.
    # Building the WASM engines requires the Rust toolchain and the wasm32-wasip1 target, which the release workflow installs before this hook runs.
    - go generate ./internal/provider/...
builds:
  - env:
      - CGO_ENABLED=0
    mod_timestamp: "{{ .CommitTimestamp }}"
    flags:
      - -trimpath
    ldflags:
      - "-s -w -X main.version={{.Version}} -X main.commit={{.Commit}}"
    goos:
      - freebsd
      - windows
      - linux
      - darwin
    goarch:
      - amd64
      - "386"
      - arm
      - arm64
    ignore:
      - goos: darwin
        goarch: "386"
    binary: "{{ .ProjectName }}_v{{ .Version }}"
archives:
  - formats: [zip]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
checksum:
  name_template: "{{ .ProjectName }}_{{ .Version }}_SHA256SUMS"
  algorithm: sha256
signs:
  - artifacts: checksum
    args:
      - "--batch"
      - "--local-user"
      - "{{ .Env.GPG_FINGERPRINT }}"
      - "--output"
      - "${signature}"
      - "--detach-sign"
      - "${artifact}"
release:
  draft: false
  prerelease: true
changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
