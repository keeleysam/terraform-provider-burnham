---
# generated by https://github.com/hashicorp/terraform-plugin-docs
# subcategory written by cmd/gendoctemplates; do not edit by hand
page_title: "color_adjust function - burnham"
subcategory: "Color"
description: |-
  Nudge a color's OKLCh channels (lighten, darken, saturate, rotate hue, fade)
---

<!--
  DO NOT EDIT. This page is generated by "go generate ./..." and any change here is overwritten on the next run.
  Edit the source instead:
    prose (MarkdownDescription): internal/provider/color/descriptions/color_adjust.md
    example (Example Usage):     examples/functions/color_adjust/function.tf
-->

# function: color_adjust

Nudges one or more OKLCh channels of a color and returns the result as hex. Because it works in OKLCh, a single function covers what would otherwise be a pile of separate operations: lighten and darken (the `lightness` channel), saturate and desaturate (`chroma`), rotate hue (`hue`), grayscale (`chroma` set to 0), and fade (`alpha`).

The `adjustments` object maps channel names (`lightness` 0-1, `chroma` >= 0, `hue` degrees, `alpha` 0-1) to values. A bare number sets the channel absolutely; a string applies an operation relative to the current value: `"+0.1"` adds, `"-0.1"` subtracts, `"*0.9"` scales down (a 10% darken when applied to lightness), `"/2"` halves. This is the chroma.js adjustment grammar.

Lightness and alpha are clamped to [0, 1], chroma to non-negative, and hue wraps modulo 360, so any adjustment yields a valid color. The headline use is deriving a full set of UI state colors (hover, active, disabled, focus ring) from one brand color in a generated theme.

## Example Usage

```terraform
// Derive a full set of UI state colors from one brand color, all in OKLCh.
locals { brand = "#3366cc" }

output "states" {
  value = {
    base     = local.brand
    hover    = provider::burnham::color_adjust(local.brand, { lightness = "*0.92" })
    active   = provider::burnham::color_adjust(local.brand, { lightness = "*0.85" })
    disabled = provider::burnham::color_adjust(local.brand, { chroma = "*0.4", lightness = "+0.1" })
    focus    = provider::burnham::color_adjust(local.brand, { hue = "+20" })
  }
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
color_adjust(color string, adjustments dynamic) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `color` (String) The color to adjust, in any CSS notation.
1. `adjustments` (Dynamic) An object of channel adjustments. Keys: `lightness` (OKLCh L, 0-1), `chroma` (OKLCh C, >= 0), `hue` (degrees), `alpha` (0-1). Each value is a number (set absolutely) or a string operation: `"+0.1"`, `"-0.1"`, `"*0.9"`, `"/2"`.
