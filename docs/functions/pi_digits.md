---
# generated by https://github.com/hashicorp/terraform-plugin-docs
# subcategory written by cmd/gendoctemplates; do not edit by hand
page_title: "pi_digits function - burnham"
subcategory: "Numerics"
description: |-
  Return the first count digits of π, modeled on the RFC 3091 https://www.rfc-editor.org/rfc/rfc3091 §1 TCP service
---

# function: pi_digits

Returns the first `count` decimal digits of π *following* the decimal point as a single ASCII string. Models the [RFC 3091 §1](https://www.rfc-editor.org/rfc/rfc3091#section-1) TCP service, which always streams "starting with the most significant digit following the decimal point" — there is no seek operation in the protocol, so this function takes only `count`, not a starting position.

Example:
- `pi_digits(10)` → `"1415926535"` (the leading 3 is implied per RFC §Note and never emitted)

**Implementation cap.** `count` > 3,141,592 (= ⌊π × 10⁶⌋) errors. See `pi_digit` for the rationale.

## Example Usage

```terraform
// pi_digits — RFC 3091 §1 TCP service for π. Returns the first `count`
// digits following the decimal point. The leading 3 of π = 3.1415… is
// implied per RFC and never emitted.
output "first_ten_digits" {
  value = provider::burnham::pi_digits(10)
  // → "1415926535"
}

output "first_thirty_digits" {
  value = provider::burnham::pi_digits(30)
  // → "141592653589793238462643383279"
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
pi_digits(count number) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `count` (Number) How many digits to return; count >= 0. Empty string if count = 0.
