---
# generated by https://github.com/hashicorp/terraform-plugin-docs
# subcategory written by cmd/gendoctemplates; do not edit by hand
page_title: "color_mix function - burnham"
subcategory: "Color"
description: |-
  Blend two colors at a given ratio (OKLCh by default)
---

<!--
  DO NOT EDIT. This page is generated by "go generate ./..." and any change here is overwritten on the next run.
  Edit the source instead:
    prose (MarkdownDescription): internal/provider/color/descriptions/color_mix.md
    example (Example Usage):     examples/functions/color_mix/function.tf
-->

# function: color_mix

Blends two colors at a given ratio and returns the result as hex. An `amount` of 0 returns the first color, 1 returns the second, and 0.5 is the midpoint; values outside [0, 1] are clamped.

Mixing happens in OKLCh by default, which interpolates hue and lightness perceptually and avoids the muddy grey midpoints of naive sRGB blending. Choose another space with `{ space = "..." }`: one of `oklch`, `oklab`, `rgb`, `hsv`, `lab`, `hcl`. This mirrors the CSS `color-mix(in <space>, a, b)` function.

Alpha is interpolated alongside the color, so mixing a translucent color yields a proportionally translucent result.

## Example Usage

```terraform
// Derive a hover shade by mixing the brand color 15% toward black (in OKLCh).
output "button_hover" {
  value = provider::burnham::color_mix("#3366cc", "#000000", 0.15)
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
color_mix(a string, b string, amount number, options dynamic...) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `a` (String) The first color (returned when amount is 0).
1. `b` (String) The second color (returned when amount is 1).
1. `amount` (Number) Blend position in [0, 1]; values outside are clamped.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Dynamic) An optional object. Key: `space` (interpolation space, default `oklch`; one of `oklch`, `oklab`, `rgb`, `hsv`, `lab`, `hcl`).
