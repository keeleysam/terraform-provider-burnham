---
# generated by https://github.com/hashicorp/terraform-plugin-docs
# subcategory written by cmd/gendoctemplates; do not edit by hand
page_title: "cidr_find_free function - burnham"
subcategory: "Networking"
description: |-
  Find the first available subnet of a given prefix length within a pool
---

# function: cidr_find_free

Finds the first free subnet of a given size inside an address pool, so you can allocate the next subnet without hardcoding offsets.

Returns the first prefix of length `prefix_len` that is available within `pool` after removing all `used` CIDRs.

This is handy for IPAM-style subnet allocation: give your VPC CIDR(s) as the `pool` list (a single CIDR still goes in as a one-element list, e.g. `["10.0.0.0/16"]`) and the list of already-allocated subnets as `used`, and it returns the next free subnet to assign to a new workload. It works well in `locals` blocks to compute the next available AZ subnet.

-> **Note:** Returns `null` if no prefix of that size is available in the pool.

## Example Usage

```terraform
// Find the first free /N within a pool, skipping already-allocated CIDRs. Returns null if no prefix of that size is available.
output "next_subnet" {
  value = provider::burnham::cidr_find_free(
    ["10.0.0.0/16"],
    ["10.0.0.0/24", "10.0.1.0/24"],
    24,
  )
  // → "10.0.2.0/24"
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
cidr_find_free(pool list of string, used list of string, prefix_len number) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `pool` (List of String) The address space to allocate from.
1. `used` (List of String) CIDRs already in use within the pool.
1. `prefix_len` (Number) The desired prefix length of the returned subnet (e.g. 24 for a /24).
