---
# generated by https://github.com/hashicorp/terraform-plugin-docs
# subcategory written by cmd/gendoctemplates; do not edit by hand
page_title: "pi_approximate_digits function - burnham"
subcategory: "Numerics"
description: |-
  Return the first count digits of 22/7, modeled on the RFC 3091 https://www.rfc-editor.org/rfc/rfc3091 §1.1 TCP approximate service
---

<!--
  DO NOT EDIT. This page is generated by "go generate ./..." and any change here is overwritten on the next run.
  Edit the source instead:
    prose (MarkdownDescription): internal/provider/numerics/descriptions/pi_approximate_digits.md
    example (Example Usage):     examples/functions/pi_approximate_digits/function.tf
-->

# function: pi_approximate_digits

<!-- Edit here: this is the MarkdownDescription source for the burnham pi_approximate_digits function. docs/functions/pi_approximate_digits.md is generated from it by "go generate ./..."; do not edit the generated doc. -->

Returns the first `count` decimal digits of 22/7 *following* the decimal point as a single ASCII string. Models the [RFC 3091 §1.1](https://www.rfc-editor.org/rfc/rfc3091#section-1.1) TCP approximate service, which streams `"starting with the most significant digit following the decimal point"`: there is no seek operation, so this function takes only `count`.

Because 22/7 is a period-6 repeating decimal, the output for any count is just `"142857"` repeated and truncated.

- `pi_approximate_digits(12)` → `"142857142857"` (the 6-digit cycle, twice)

~> **Note:** `count` greater than 3,141,592 (= ⌊π × 10⁶⌋) errors, matching `pi_digits`. At one byte per digit the output can never exceed a few megabytes, keeping both `pi_approximate_digits` and `pi_digits` bounded at plan time.

## Example Usage

```terraform
// pi_approximate_digits: RFC 3091 §1.1 TCP approximate service for 22/7. Returns the first `count` digits of 22/7 = 3.142857142857… (period-6).
output "two_cycles" {
  value = provider::burnham::pi_approximate_digits(12)
  // → "142857142857"
}

output "partial_cycle" {
  value = provider::burnham::pi_approximate_digits(5)
  // → "14285"
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
pi_approximate_digits(count number) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `count` (Number) How many digits to return; count >= 0. Empty string if count = 0.
