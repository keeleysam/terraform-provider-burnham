---
# generated by https://github.com/hashicorp/terraform-plugin-docs
# subcategory written by cmd/gendoctemplates; do not edit by hand
page_title: "percentile function - burnham"
subcategory: "Numerics"
description: |-
  Percentile of a list of numbers, by linear interpolation
---

# function: percentile

Returns the `p`-th percentile of `numbers` using linear interpolation between adjacent ordered values. This is **Hyndman & Fan Type 7** — the default method in [NumPy](https://numpy.org/doc/stable/reference/generated/numpy.percentile.html), R, and Excel's `PERCENTILE.INC`.

Definition: let the sorted observations be `x[0] ≤ x[1] ≤ … ≤ x[N-1]`. Compute `h = (p / 100) × (N - 1)`. If `h` is an integer, return `x[h]`. Otherwise return `x[⌊h⌋] + (h - ⌊h⌋) × (x[⌈h⌉] - x[⌊h⌋])`.

Valid `p` is in `[0, 100]`. `p = 0` returns the minimum; `p = 100` returns the maximum; `p = 50` matches `median(numbers)`.

## Example Usage

```terraform
/*
Linear-interpolation percentile (Hyndman & Fan Type 7) — same definition as numpy.percentile, R's default, and Excel's PERCENTILE.INC.

Use it to set thresholds based on observed distributions, e.g. "scale instance
count to handle the p95 of last week's request rate" without hand-rolling a
sort + index in HCL.
*/
locals {
  request_rates = [120, 145, 132, 158, 199, 180, 175, 165, 162, 211]
}

output "p50" {
  value = provider::burnham::percentile(local.request_rates, 50)
}

output "p95" {
  value = provider::burnham::percentile(local.request_rates, 95)
  // h = 0.95 * 9 = 8.55 → between sorted[8]=199 and sorted[9]=211 → 199 + 0.55*12 = 205.6
}

output "p100_is_max" {
  value = provider::burnham::percentile(local.request_rates, 100)
  // → 211
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
percentile(numbers list of number, p number) number
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `numbers` (List of Number) A non-empty list of numbers.
1. `p` (Number) The percentile to compute, in [0, 100].
