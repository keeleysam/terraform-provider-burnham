---
# generated by https://github.com/hashicorp/terraform-plugin-docs
# subcategory written by cmd/gendoctemplates; do not edit by hand
page_title: "dotenvdecode function - burnham"
subcategory: "Structured Data"
description: |-
  Parse a dotenv (.env) file into a string-to-string map
---

# function: dotenvdecode

Parses a [dotenv](https://github.com/joho/godotenv) (`.env`) file body into an object whose attributes are the file's keys, with all values as strings. Comments (`#`) are ignored. Both `KEY=value` and `export KEY=value` lines are accepted. Double-quoted values support `\n`, `\r`, `\t` and `${VAR}` interpolation against earlier keys; single-quoted values are taken literally.

All values are returned as strings — dotenv has no type system. Cast on the Terraform side with `tonumber()` / `tobool()` if needed.

Backed by [joho/godotenv](https://github.com/joho/godotenv), the canonical Go implementation.

**Common uses:** ingesting environment files for ECS/Lambda task definitions, container env blocks, or shipping config alongside compiled artifacts.

## Example Usage

```terraform
// Parse a .env / dotenv body into a string-to-string object. All values are strings.
output "env" {
  value = provider::burnham::dotenvdecode("# comment\nDATABASE_URL=postgres://localhost\nLOG_LEVEL=info\n")
  // → { DATABASE_URL = "postgres://localhost", LOG_LEVEL = "info" }
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
dotenvdecode(input string) dynamic
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `input` (String) A dotenv-formatted string.
