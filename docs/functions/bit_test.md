---
# generated by https://github.com/hashicorp/terraform-plugin-docs
# subcategory written by cmd/gendoctemplates; do not edit by hand
page_title: "bit_test function - burnham"
subcategory: "Numerics"
description: |-
  Report whether bit i of value is set
---

# function: bit_test

Returns `true` if bit `i` of `value` is set, `false` otherwise. Bit `i` has place value `2^i`, so `bit_test(8, 3) = true` and `bit_test(8, 0) = false`.

Handy for reading a single flag out of a bitmask: `bit_test(flags, 4)`.

-> `value` and `i` must be integers and `i` must be `>= 0`. A negative index, or a non-integral or infinite argument, is an error.

## Example Usage

```terraform
// Report whether a single bit is set.
output "bit_three_set" {
  value = provider::burnham::bit_test(8, 3)
  // → true
}

output "bit_zero_set" {
  value = provider::burnham::bit_test(8, 0)
  // → false
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
bit_test(value number, i number) bool
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `value` (Number) The integer to test.
1. `i` (Number) The zero-based bit index to test; must be >= 0.
