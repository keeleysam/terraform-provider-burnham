---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "ndjsonencode function - burnham"
subcategory: ""
description: |-
  Encode a list of values as NDJSON
---

# function: ndjsonencode

Encodes a list as [NDJSON](https://github.com/ndjson/ndjson-spec) — one JSON value per line, joined with `\n`, with a single trailing newline. Each element is JSON-encoded with sorted object keys and integer-rendering for whole-number values, matching the conventions of `jsonencode` here.

**Common uses:** generating fixture log files for tests, materializing event feeds, building bulk-import payloads.

## Example Usage

```terraform
// Encode a list as NDJSON (one JSON value per line, trailing newline).
output "log_lines" {
  value = provider::burnham::ndjsonencode([
    { id = 1, msg = "started" },
    { id = 2, msg = "ready" },
  ])
}
/* →
{"id":1,"msg":"started"}
{"id":2,"msg":"ready"}
*/
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
ndjsonencode(value dynamic) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `value` (Dynamic) A list (tuple) of values to encode, one per line.
