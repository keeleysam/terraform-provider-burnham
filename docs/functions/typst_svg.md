---
# generated by https://github.com/hashicorp/terraform-plugin-docs
# subcategory written by cmd/gendoctemplates; do not edit by hand
page_title: "typst_svg function - burnham"
subcategory: "Documents"
description: |-
  Render a Typst document to SVG strings, one per page
---

<!--
  DO NOT EDIT. This page is generated by "go generate ./..." and any change here is overwritten on the next run.
  Edit the source instead:
    prose (MarkdownDescription): internal/provider/documents/descriptions/typst_svg.md
    example (Example Usage):     examples/functions/typst_svg/function.tf
-->

# function: typst_svg

Renders a [Typst](https://typst.app) document to SVG, one per page, returned as a list of SVG strings (a single-page document is just `result[0]`). The SVG is returned as text, not base64, so you can use it directly or feed it to `svg_render` to rasterize at any scale.

Pass structured data through the `inputs` option: it is exposed to the document as `sys.inputs` as native Typst values, so the document reads `sys.inputs.customer.name` directly with no decoding. Use `files` (a map of path to base64-encoded content) for `#import`ed modules and `#image` assets, and `fonts` (a list of base64-encoded fonts) for families beyond the bundled Noto and Liberation sets.

Typst runs as WebAssembly under a pure-Go runtime, so the provider stays CGO-free and the output is deterministic across operating systems and architectures. The one exception is deliberate: a document that calls a non-deterministic Typst builtin such as `datetime.today()` will produce different output on different days.

## Example Usage

```terraform
# A two-page document rendered to SVG. typst_svg returns one SVG string per page,
# as text (not base64), so you can use it directly or feed it to svg_render.
locals {
  report = <<-TYPST
    #set text(font: "Noto Serif")
    #set page(width: 240pt, height: 160pt, margin: 20pt)
    = Page one
    Summary for #sys.inputs.team.
    #pagebreak()
    = Page two
    Details follow.
  TYPST
}

output "report_svgs" {
  value = provider::burnham::typst_svg(local.report, {
    inputs = { team = "Platform" }
  })
  # → a list of two SVG strings, one per page
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
typst_svg(source string, options dynamic...) list of string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `source` (String) The Typst markup to render.
<!-- variadic argument generated by tfplugindocs -->
1. `options` (Variadic, Dynamic) An optional object with keys: `inputs`, `files`, and `fonts`.
