---
# generated by https://github.com/hashicorp/terraform-plugin-docs
# subcategory written by cmd/gendoctemplates; do not edit by hand
page_title: "x509_fingerprint function - burnham"
subcategory: "Cryptography"
description: |-
  Hex fingerprint of an X.509 certificate's DER bytes
---

# function: x509_fingerprint

Returns the hex-encoded `algorithm` digest of the first `CERTIFICATE` block's DER bytes — the same value `openssl x509 -fingerprint -<algorithm>` produces, minus the colon separators between byte pairs.

`algorithm` is one of `"sha1"`, `"sha256"`, `"sha384"`, `"sha512"`. (`sha224` is not commonly used for fingerprints and is omitted.) `sha256` is the standard choice in 2026; `sha1` is supported only for compatibility with older systems.

## Example Usage

```terraform
// Hex fingerprint of a certificate's DER bytes. Same value as `openssl x509 -fingerprint -sha256` minus the colons.
variable "tls_cert_pem" { type = string }

output "sha256_pin" {
  value = provider::burnham::x509_fingerprint(var.tls_cert_pem, "sha256")
  // → e.g. "6d2d325a319a26c8d89f417fc543d2673bdce5f9ba2e4ae2bdc6f409f0e346cc"
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
x509_fingerprint(pem string, algorithm string) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `pem` (String) PEM-encoded certificate (or a bundle containing one).
1. `algorithm` (String) Hash algorithm: "sha1", "sha256", "sha384", or "sha512".
