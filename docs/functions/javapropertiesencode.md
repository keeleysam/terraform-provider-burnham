---
# generated by https://github.com/hashicorp/terraform-plugin-docs
# subcategory written by cmd/gendoctemplates; do not edit by hand
page_title: "javapropertiesencode function - burnham"
subcategory: "Structured Data"
description: |-
  Encode a string-keyed object as a Java .properties file body
---

# function: javapropertiesencode

Encodes a flat string-keyed object as `key=value` lines in alphabetical key order, ready to write to disk. Numeric and boolean values are stringified. Keys and values are escaped according to `java.util.Properties` rules: leading whitespace, `=`, `:`, `#`, `!`, `\`, and control characters are backslash-escaped; non-ASCII characters are emitted as `\uXXXX` escapes for portability across legacy ISO-8859-1 readers.

Output is hand-formatted rather than written via the magiconair/properties library writer, so that keys are sorted (the library preserves insertion order) and the output has no leading metadata block. Nested objects and lists are not allowed.

## Example Usage

```terraform
// Encode a flat object as a Java .properties body. Keys sorted; non-ASCII characters emitted as \uXXXX for portability.
output "props" {
  value = provider::burnham::javapropertiesencode({
    "app.name"     = "frontend"
    "app.replicas" = 3
  })
}
/* →
app.name=frontend
app.replicas=3
*/
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
javapropertiesencode(value dynamic) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `value` (Dynamic) A flat object whose attributes are the property keys; values must be primitives.
