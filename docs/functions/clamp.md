---
# generated by https://github.com/hashicorp/terraform-plugin-docs
# subcategory written by cmd/gendoctemplates; do not edit by hand
page_title: "clamp function - burnham"
subcategory: "Numerics"
description: |-
  Clamp value into the closed interval [min_val, max_val]
---

<!--
  DO NOT EDIT. This page is generated by "go generate ./..." and any change here is overwritten on the next run.
  Edit the source instead:
    prose (MarkdownDescription): internal/provider/numerics/descriptions/clamp.md
    example (Example Usage):     examples/functions/clamp/function.tf
-->

# function: clamp

<!-- Edit here: this is the MarkdownDescription source for the burnham clamp function. docs/functions/clamp.md is generated from it by "go generate ./..."; do not edit the generated doc. -->

Returns `value` if it falls within `[min_val, max_val]`, `min_val` if `value < min_val`, and `max_val` if `value > max_val`. Equivalent to `max(min_val, min(max_val, value))` but easier to read and harder to get backwards.

Errors when `min_val > max_val`: the interval is empty in that case and any return value would be a guess. Both bounds are inclusive.

## Example Usage

```terraform
// Bound a number to a closed interval. Equivalent to max(min_val, min(max_val, value)) but reads cleanly.
output "in_range" {
  value = provider::burnham::clamp(5, 0, 10)
  // → 5
}

output "below_min" {
  value = provider::burnham::clamp(-3, 0, 10)
  // → 0
}

output "above_max" {
  value = provider::burnham::clamp(99, 0, 10)
  // → 10
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
clamp(value number, min_val number, max_val number) number
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `value` (Number) The value to clamp.
1. `min_val` (Number) Lower bound (inclusive).
1. `max_val` (Number) Upper bound (inclusive); must be >= min_val.
