---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "cidr_usable_host_count function - burnham"
subcategory: ""
description: |-
  Return the number of usable host addresses in a CIDR
---

# function: cidr_usable_host_count

Returns the number of usable host addresses in the CIDR. For IPv4, the network and broadcast addresses are subtracted, with special cases: `/31` returns 2 (point-to-point, RFC 3021), `/32` returns 1 (host route). For IPv6, all addresses are considered usable.

**Common uses:** asserting a subnet is large enough for a given number of workloads without manually subtracting 2 everywhere; sizing auto-scaling groups or node pools based on the actual available IP space in the target subnet.

## Example Usage

```terraform
// Usable host count: subtracts network + broadcast for IPv4. RFC-correct edge cases: /31 = 2 (point-to-point), /32 = 1.
output "usable" {
  value = provider::burnham::cidr_usable_host_count("10.0.0.0/24")
  // → 254
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
cidr_usable_host_count(cidr string) number
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `cidr` (String) The CIDR to count usable hosts in.
