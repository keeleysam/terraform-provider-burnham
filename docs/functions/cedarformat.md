---
# generated by https://github.com/hashicorp/terraform-plugin-docs
# subcategory written by cmd/gendoctemplates; do not edit by hand
page_title: "cedarformat function - burnham"
subcategory: "Expression Languages"
description: |-
  Canonicalize a Cedar policy document
---

<!--
  DO NOT EDIT. This page is generated by "go generate ./..." and any change here is overwritten on the next run.
  Edit the source instead:
    prose (MarkdownDescription): internal/provider/cedar/descriptions/cedarformat.md
    example (Example Usage):     examples/functions/cedarformat/function.tf
-->

# function: cedarformat

<!-- Edit here: this is the MarkdownDescription source for the burnham cedarformat function. docs/functions/cedarformat.md is generated from it by "go generate ./..."; do not edit the generated doc. -->

Parses a [Cedar](https://www.cedarpolicy.com) policy document and returns its canonical text serialization: normalized layout and indentation, with statements kept in their input order. It fails the plan on syntactically invalid input (use `cedarvalidate` for a non-failing boolean check).

Comments are dropped and formatting is normalized (each policy is re-rendered from the parsed AST); annotations such as `@id(...)` are preserved. The output is stable and idempotent, so two documents that differ only in whitespace or comments canonicalize to the same string. Backed by [cedar-go](https://github.com/cedar-policy/cedar-go), the official Go implementation of Cedar.

## Example Usage

```terraform
// cedarformat: canonicalize a hand-written Cedar policy, normalizing layout and
// indentation. Fails on invalid input (use cedarvalidate for a bool check).
output "canonical" {
  value = provider::burnham::cedarformat("permit(principal==User::\"alice\",action==Action::\"view\",resource);")
  /* →
  permit (
      principal == User::"alice",
      action == Action::"view",
      resource
  );
  */
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
cedarformat(policies string) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `policies` (String) A Cedar policy document to canonicalize.
