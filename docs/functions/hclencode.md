---
# generated by https://github.com/hashicorp/terraform-plugin-docs
# subcategory written by cmd/gendoctemplates; do not edit by hand
page_title: "hclencode function - burnham"
subcategory: "Structured Data"
description: |-
  Encode an object as an HCL attribute body
---

<!--
  DO NOT EDIT. This page is generated by "go generate ./..." and any change here is overwritten on the next run.
  Edit the source instead:
    prose (MarkdownDescription): internal/provider/dataformat/descriptions/hclencode.md
    example (Example Usage):     examples/functions/hclencode/function.tf
-->

# function: hclencode

<!-- Edit here: this is the MarkdownDescription source for the burnham hclencode function. docs/functions/hclencode.md is generated from it by "go generate ./..."; do not edit the generated doc. -->

Encodes a Terraform object as a sequence of HCL attribute statements (`key = value` lines), one per object member, in alphabetical key order.

Values render in their natural HCL form:

- Nested objects become HCL object literals (`{ ... }`).
- Lists become bracketed sequences.
- Primitives (strings, numbers, bools, null) render as HCL literals.

Output is formatted with `hclwrite.Format`, matching Terraform's canonical formatting.

~> **Note:** This is not the same as `provider::terraform::encode_tfvars`, which is `.tfvars`-specific. Use `hclencode` for emitting general-purpose HCL config files.

## Example Usage

```terraform
// Encode an object as HCL attribute statements (one per member, alphabetical order).
output "hcl" {
  value = provider::burnham::hclencode({
    name     = "web"
    replicas = 3
    tags     = ["prod", "east"]
  })
}
/* →
name     = "web"
replicas = 3
tags     = ["prod", "east"]
*/
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
hclencode(value dynamic) string
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `value` (Dynamic) An object to render as HCL.
