---
# generated by https://github.com/hashicorp/terraform-plugin-docs
# subcategory written by cmd/gendoctemplates; do not edit by hand
page_title: "pluscode_decode function - burnham"
subcategory: "Geographic"
description: |-
  Decode a Plus code into centre point, bounding box, and length
---

<!--
  DO NOT EDIT. This page is generated by "go generate ./..." and any change here is overwritten on the next run.
  Edit the source instead:
    prose (MarkdownDescription): internal/provider/geographic/descriptions/pluscode_decode.md
    example (Example Usage):     examples/functions/pluscode_decode/function.tf
-->

# function: pluscode_decode

<!-- Edit here: this is the MarkdownDescription source for the burnham pluscode_decode function. docs/functions/pluscode_decode.md is generated from it by "go generate ./..."; do not edit the generated doc. -->

Parses a full Open Location Code and returns:

- `latitude` / `longitude`: the centre of the cell, in degrees.
- `lat_min` / `lat_max` / `lon_min` / `lon_max`: the cell's bounding box.
- `length`: the code's nominal length.

Errors on short codes (those that need a reference location). Recover a short code to a full code against a reference location outside Terraform (any Open Location Code library exposes a recover-nearest operation) and pass the resulting full code in.

## Example Usage

```terraform
// Decode a full Plus code into centre + bounding box. Short codes (those that need a reference location) are not supported here, so pre-extend them upstream of Terraform.
output "civic_center_decoded" {
  value = provider::burnham::pluscode_decode("849VQHFJ+X6")
  // → { latitude = …, longitude = …, lat_min/max = …, lon_min/max = …, length = 10 }
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
pluscode_decode(code string) object
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `code` (String) The Plus code to decode (full code, including the `+`).
