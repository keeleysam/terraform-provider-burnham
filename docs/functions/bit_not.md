---
# generated by https://github.com/hashicorp/terraform-plugin-docs
# subcategory written by cmd/gendoctemplates; do not edit by hand
page_title: "bit_not function - burnham"
subcategory: "Numerics"
description: |-
  Complement of a value within an unsigned field of a given bit width
---

<!--
  DO NOT EDIT. This page is generated by "go generate ./..." and any change here is overwritten on the next run.
  Edit the source instead:
    prose (MarkdownDescription): internal/provider/numerics/descriptions/bit_not.md
    example (Example Usage):     examples/functions/bit_not/function.tf
-->

# function: bit_not

<!-- Edit here: this is the MarkdownDescription source for the burnham bit_not function. docs/functions/bit_not.md is generated from it by "go generate ./..."; do not edit the generated doc. -->

Returns the bitwise complement of `value` within an unsigned field of `bits` bits: `value XOR (2^bits - 1)`. Every bit of the field is flipped, so `bit_not(0, 8) = 255`, `bit_not(255, 8) = 0`, and `bit_not(1, 4) = 14`.

The width is required on purpose. A width-less NOT of an integer is infinite in two's-complement (`~0` is `-1`, an endless string of ones), which is almost never what a configuration wants. Pinning the field width makes the result a plain unsigned integer.

-> `bits` must be `>= 1` and `value` must satisfy `0 <= value < 2^bits`. A value outside that range (for example `bit_not(256, 8)`) is an error, as is a non-integral or infinite argument.

## Example Usage

```terraform
// Complement within an 8-bit field: every bit of the byte flips.
output "inverted_byte" {
  value = provider::burnham::bit_not(0, 8)
  // → 255
}

// Complement within a 4-bit nibble.
output "inverted_nibble" {
  value = provider::burnham::bit_not(1, 4)
  // → 14
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
bit_not(value number, bits number) number
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `value` (Number) The value to complement; must satisfy 0 <= value < 2^bits.
1. `bits` (Number) The field width in bits; must be >= 1.
