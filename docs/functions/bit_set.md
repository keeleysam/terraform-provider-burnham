---
# generated by https://github.com/hashicorp/terraform-plugin-docs
# subcategory written by cmd/gendoctemplates; do not edit by hand
page_title: "bit_set function - burnham"
subcategory: "Numerics"
description: |-
  Set bit i of value to 1
---

# function: bit_set

Returns `value` with bit `i` set to 1. Bit `i` has place value `2^i`, so `bit_set(0, 3) = 8`. Setting a bit that is already set is a no-op.

Handy for flag manipulation: `bit_set(flags, 4)` turns on the bit at index 4 without disturbing the others.

-> `value` and `i` must be integers and `i` must be `>= 0`. A negative index, or a non-integral or infinite argument, is an error.

## Example Usage

```terraform
// Set bit 3 (place value 2^3 = 8).
output "flag_on" {
  value = provider::burnham::bit_set(0, 3)
  // → 8
}
```

## Signature

<!-- signature generated by tfplugindocs -->
```text
bit_set(value number, i number) number
```

## Arguments

<!-- arguments generated by tfplugindocs -->
1. `value` (Number) The integer to modify.
1. `i` (Number) The zero-based bit index to set; must be >= 0.
